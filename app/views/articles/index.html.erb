<div class="index">
  <h1>Our anonimous articles</h1>
  <ul class="mt-4">
    <% @articles.each do |article| %>
      <li> <strong><%= link_to article.title, article_path(article) %> </strong><br>
      <em>"<%= article.content %>"</em></li><br>
    <% end %>
  </ul>
</div>
